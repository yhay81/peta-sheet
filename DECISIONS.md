# Decisions

## 2026-07-26 — 端末内で実寸ラベルを作る

- 初期版は利用頻度の高い12面、24面、65面の3形状に絞る
- ブランドや商品番号の互換を断定せず、幅、高さ、列数、行数を表示してパッケージの寸法図との照合を促す
- CSSのmm単位と`@page size: A4; margin: 0`を使い、印刷倍率100%を案内する
- 0.1mm単位、±5mmの左右・上下補正を用意し、普通紙での重ね合わせテストを案内する
- Excel貼り付けはタブ、CSVは引用符を含むカンマ区切りを解釈し、複数ページを一度に印刷する
- 入力文字と印刷設定は`localStorage`だけに保存する
- Better Authは所有者アカウントも同期も不要なため採用しない
- D1へ保存するのは匿名ファネルのイベント名だけで、ラベル文字と設定値は送らない
